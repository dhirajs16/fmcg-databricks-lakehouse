[project]
name = "fmcg-databricks-lakehouse"
version = "1.0.0"
description = "Databricks Medallion pipeline for an FMCG acquisition scenario"
requires-python = ">=3.10"
authors = [{name = "YOUR_NAME"}]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]

[tool.ruff]
line-length = 100
target-version = "py310"
